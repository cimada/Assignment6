#if !defined(ADDRESS)
#define ADDRESS
#include <iostream>

using namespace std;

/*class Address
{
private:
	char street[51];
	char city[26];
	char state[3];
	char zip[6];
public:
	Address(char* pstreet, char* pcity, char* pstate, char* pzip);
};
*/
#endif